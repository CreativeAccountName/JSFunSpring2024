let planets = ["Pluto", "Saturn", "Mar"]; // e.g.

/**
 * Create a variable called "length".
 * It should be equal to the length of "planets".
 */

// WRITE YOUR ANSWER BELOW THIS LINE

// Uses .length to get the number of items in the array and stores them in the length variable.
let length = planets.length;
console.log(length);